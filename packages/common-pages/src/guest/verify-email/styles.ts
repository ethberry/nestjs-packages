import {createStyles, makeStyles} from "@material-ui/core";

export default makeStyles(
  () =>
    createStyles({
      popup: {
        width: 400,
        margin: "auto",
      },
    }),
  {name: "VerifyEmail"},
);
