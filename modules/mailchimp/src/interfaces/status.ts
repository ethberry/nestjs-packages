// not exported from mailchimp since 3.0.3
export enum Status {
  subscribed = "subscribed",
  unsubscribed = "unsubscribed",
  cleaned = "cleaned",
  pending = "pending",
  transactional = "transactional",
}
